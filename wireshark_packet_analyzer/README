#Wireshark Packet Analyzer

->This script utilizes the wireshark 'Export Packet Dissections --> As 'Plain Text' File' Feature.
	->Script takes in  generated plain text file and passes it to a python script that 
		-> manipulates it to create a file that contains variations of 
			->ASCII, HEX, and PACKET HEADERS of sniffed packets. 

->USAGE:
        echo "Usage: Make Sure To Use ABSOLUTE PATH."
        echo "Usage: [SCRIPT_NAME] [OPTIONS] [PATH_TO_PYTHON_FILE] [PATH_TO_ASCII_FILE] [PATH_TO_OUTPUT_FILE]"
        echo "Usage: All Options Specified Will Print To Output File Specified. "
        echo "OPTIONS: "
        echo "        -r)..unsorted raw hex and ascii."
        echo "        -R)..sorted raw hex and ascii."
        echo "        -a)..unsorted raw ascii only."
        echo "        -A)..sorted raw ascii only."
        echo "        -h)..unsorted raw hex only."
        echo "        -H)..sorted hex only."
        echo "        -p)..unsorted packet headers only."
        echo "        -P)..sorted packet headers."
